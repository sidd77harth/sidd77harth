from google.colab import files data = files.upload()
import csv
import numpy as np path=r"/content/diabetes (1).csv" with open(path,'r') as f:
reader = csv.reader(f,delimiter = ",") headers = next(reader)
data = list(reader)
data = np.array(data).astype(float) data

from numpy import loadtxt from numpy import genfromtxt path = r"/content/Iris.csv" datapath= open(path,'r')
data = genfromtxt(datapath) print(data.shape) print(data[:3])
 
from pandas import read_csv
path = r"C:\pima-indians-diabetes.csv"
headernames = ['preg', 'plas', 'pres', 'skin', 'test', 'mass', 'pedi', 'age', 'class'] data = read_csv(path, names=headernames)
print(data.shape) print(data[:3])
